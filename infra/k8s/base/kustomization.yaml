apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - backend-deployment.yaml
  - ai-worker-deployment.yaml

commonLabels:
  app.kubernetes.io/part-of: taxwijs
  app.kubernetes.io/managed-by: kustomize
